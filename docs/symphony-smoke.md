# Symphony Smoke

This note was created by a Symphony-routed Codex run to verify the Linear label and repository routing loop for `anmho/symphony`.

GitHub App PR identity smoke coverage confirms Symphony can open pull requests as the installed app bot.
